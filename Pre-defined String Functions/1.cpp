#include<stdio.h>
#include<string.h>
int main(void) {
    char str[50] = "Hello CodeChum!";
    int bar;
    
    bar = strlen(str);
    printf("%s %i", str, bar);
    
    // Hey there, start typing your C code here...
}
